@model IEnumerable<MaintenanceRecords.Models.MaintRecordListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Records</h2>

<p>
    @Html.ActionLink("Create Record", "Create")
</p>
@if (TempData.ContainsKey("SaveResult"))
{
<div class="alert alert-success fade in" role="alert">

    <button type="button" class="close" data-dismiss="alert">
        <span>&times;</span>
    </button>
    @TempData["SaveResult"]
</div>

}
    <table class="table">
       
    <tr>
        @*<th>
                @Html.DisplayNameFor(model => model.RecordId)
            </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemId)
        </th>
            *@
        <th>
            @Html.DisplayNameFor(model => model.MaintItem.ItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecordText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecordDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.RecordId)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemId)
            </td>
                *@
            <td>
                @Html.DisplayFor(modelItem => item.MaintItem.ItemName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordDate)
            </td>
            <td>
                <a href="@Url.Action("Edit", "MaintRecord", new { id = item.RecordId })" title="Edit" , rel="nofollow">
                    <img src="@Url.Content("~/Content/Images/edit-16.png")" /></a> |
                @*@Html.ActionLink("Edit", "Edit", new { id = item.RecordId }) |*@
                <a href="@Url.Action("Details", "MaintRecord", new { id = item.RecordId })" title="Details" , rel="nofollow">
                    <img src="@Url.Content("~/Content/Images/details-16.png")" /></a> |
                @*@Html.ActionLink("Details", "Details", new { id = item.RecordId }) |*@
                <a href="@Url.Action("Delete", "MaintRecord", new { id = item.RecordId })" title="Remove" , rel="nofollow">
                    <img src="@Url.Content("~/Content/Images/delete-16.png")" /></a> |
                @*@Html.ActionLink("Delete", "Delete", new { id = item.RecordId }) |*@
                <a href="@Url.Action("Show", "MaintRecord", new { id = item.MaintItem.ItemId })" title="View All" , rel="nofollow">
                    <img src="@Url.Content("~/Content/Images/view-all-16.png")" />

                    @*@Html.ActionLink($"Show all " + item.MaintItem.ItemName + " records", "../MaintRecord/show", new { id = item.MaintItem.ItemId })*@
            </td>
        </tr>
    }

</table>
